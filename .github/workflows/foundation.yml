name: Foundation Repository Gates

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "23 5 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  foundation:
    uses: nabhold/shared/.github/workflows/foundation-repository-gates.yml@b6002f94717497034e8a9a7ba61119995985db3a
    with:
      dockerfile: "Dockerfile"
      build_context: "."
      dependency_review_enabled: true
